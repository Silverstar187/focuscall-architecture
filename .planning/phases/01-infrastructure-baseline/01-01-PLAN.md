---
phase: 01-infrastructure-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/pwrunltd/focuscall-ai-frontend/.env.local
autonomous: false
requirements:
  - INFRA-03
  - AUTH-03

must_haves:
  truths:
    - "Webhook receiver survives VPS reboot — systemctl is-enabled returns 'enabled'"
    - ".env.local WEBHOOK_SECRET contains the real 64-char hex secret, not empty string"
    - ".env.local VPS_WEBHOOK_URL points to http://91.99.209.45:9000/provision, not the broken DNS name"
    - "Logged-in user sees only their own agents on the dashboard (RLS policy active on user_agents)"
  artifacts:
    - path: "/Users/pwrunltd/focuscall-ai-frontend/.env.local"
      provides: "Correct WEBHOOK_SECRET and VPS_WEBHOOK_URL values"
      contains: "WEBHOOK_SECRET=c9109218c9e92c1f09de3e695bc127b9cd83ab4372ee45d733570fc7b23fa52f"
  key_links:
    - from: ".env.local"
      to: "VPS webhook-receiver"
      via: "VPS_WEBHOOK_URL value"
      pattern: "VPS_WEBHOOK_URL=http://91\\.99\\.209\\.45:9000/provision"
---

<objective>
Fix infrastructure baseline: enable systemd persistence for webhook receiver, patch broken .env.local values, and verify Supabase RLS on user_agents table so AUTH-03 is satisfied.

Purpose: Without these fixes, a VPS reboot kills the webhook receiver, the frontend cannot reach the VPS (wrong URL), and agent list data isolation is unverified.
Output: Enabled systemd service, corrected .env.local, verified RLS policy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-baseline/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch .env.local with correct WEBHOOK_SECRET and VPS_WEBHOOK_URL</name>
  <files>/Users/pwrunltd/focuscall-ai-frontend/.env.local</files>
  <read_first>
    - /Users/pwrunltd/focuscall-ai-frontend/.env.local
  </read_first>
  <action>
In `/Users/pwrunltd/focuscall-ai-frontend/.env.local`, make exactly two line replacements. Do NOT touch any other lines.

1. Replace the line:
   `WEBHOOK_SECRET=`
   with:
   `WEBHOOK_SECRET=c9109218c9e92c1f09de3e695bc127b9cd83ab4372ee45d733570fc7b23fa52f`

2. Replace the line:
   `VPS_WEBHOOK_URL=https://vps.focuscall.ai/provision`
   with:
   `VPS_WEBHOOK_URL=http://91.99.209.45:9000/provision`

Do NOT modify any other lines. The file has 24 lines total — the result must also have 24 lines. The NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, DEEPSEEK_API_KEY, DEEPGRAM_API_KEY, SUPABASE_PAT lines must remain untouched.
  </action>
  <verify>
    <automated>grep -c "WEBHOOK_SECRET=c9109218c9e92c1f09de3e695bc127b9cd83ab4372ee45d733570fc7b23fa52f" /Users/pwrunltd/focuscall-ai-frontend/.env.local && grep -c "VPS_WEBHOOK_URL=http://91.99.209.45:9000/provision" /Users/pwrunltd/focuscall-ai-frontend/.env.local && test $(wc -l < /Users/pwrunltd/focuscall-ai-frontend/.env.local) -ge 20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "WEBHOOK_SECRET=c9109218c9e92c1f09de3e695bc127b9cd83ab4372ee45d733570fc7b23fa52f" .env.local` returns exactly 1 match
    - `grep "VPS_WEBHOOK_URL=http://91.99.209.45:9000/provision" .env.local` returns exactly 1 match
    - `grep "WEBHOOK_SECRET=$" .env.local` returns 0 matches (empty value gone)
    - `grep "vps.focuscall.ai" .env.local` returns 0 matches (broken DNS gone)
    - All other env vars unchanged (DEEPSEEK_API_KEY, DEEPGRAM_API_KEY, SUPABASE_PAT lines identical)
  </acceptance_criteria>
  <done>WEBHOOK_SECRET has real 64-char hex value; VPS_WEBHOOK_URL points to http://91.99.209.45:9000/provision; no other lines changed.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Enable systemd persistence and verify Supabase RLS</name>
  <what-built>Task 1 patched .env.local. Now two manual actions are needed that require SSH access to VPS and Supabase dashboard access.</what-built>
  <how-to-verify>
**Action A — Enable systemd service on VPS (INFRA-03):**

```bash
ssh -i ~/.ssh/openclaw_nopass root@91.99.209.45 "systemctl enable focuscall-webhook && systemctl is-enabled focuscall-webhook && systemctl status focuscall-webhook --no-pager"
```

Expected output must contain:
- `enabled` from `is-enabled`
- `active (running)` from `status`

**Action B — Verify Supabase RLS on user_agents table (AUTH-03):**

1. Open Supabase Dashboard: https://supabase.com/dashboard/project/ahdzxabztewqqdajjtsn
2. Go to Table Editor > user_agents
3. Click the shield icon (RLS) or go to Authentication > Policies
4. Verify that RLS is ENABLED on the `user_agents` table
5. Verify a SELECT policy exists with condition: `auth.uid() = user_id`

If the policy is MISSING, run this SQL in the Supabase SQL Editor:

```sql
ALTER TABLE user_agents ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users can view own agents"
  ON user_agents
  FOR SELECT
  USING (auth.uid() = user_id);

CREATE POLICY "Users can insert own agents"
  ON user_agents
  FOR INSERT
  WITH CHECK (auth.uid() = user_id);

CREATE POLICY "Users can update own agents"
  ON user_agents
  FOR UPDATE
  USING (auth.uid() = user_id);

CREATE POLICY "Users can delete own agents"
  ON user_agents
  FOR DELETE
  USING (auth.uid() = user_id);
```

Report back:
- Was systemctl enable successful? (paste output)
- Was RLS already enabled, or did you have to add the policies?
  </how-to-verify>
  <resume-signal>Type "done" with the systemctl output and RLS status, or describe any issues.</resume-signal>
</task>

</tasks>

<verification>
1. `grep "WEBHOOK_SECRET=c9109218" /Users/pwrunltd/focuscall-ai-frontend/.env.local` returns a match
2. `grep "VPS_WEBHOOK_URL=http://91.99.209.45:9000/provision" /Users/pwrunltd/focuscall-ai-frontend/.env.local` returns a match
3. User confirms `systemctl is-enabled focuscall-webhook` outputs `enabled`
4. User confirms RLS policy exists on user_agents with `auth.uid() = user_id`
</verification>

<success_criteria>
- .env.local has correct WEBHOOK_SECRET (64-char hex) and VPS_WEBHOOK_URL (http://91.99.209.45:9000/provision)
- focuscall-webhook service is enabled for boot persistence
- user_agents table has RLS with per-user SELECT policy
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-baseline/01-01-SUMMARY.md`
</output>
